import { createUseStyles } from "react-jss";

export const useSkillsStyle = createUseStyles({
    skillsSection: { display: 'flex', flexDirection: 'column' },

    skillCardsContainer: {
        display: "flex",
        justifyContent: "center",
    },

});
